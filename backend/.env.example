# Server Configuration
PORT=5000
NODE_ENV=development

# SQLite Database Configuration
# Optional: Specify custom database path (default: backend/data/eeu-cafe.db)
# DB_PATH=./data/eeu-cafe.db

# Admin Authentication
ADMIN_KEY=your-secret-admin-key-change-this

# Frontend URL (for CORS in production)
# FRONTEND_URL=https://your-frontend-domain.com
# Or multiple URLs (comma-separated):
# FRONTEND_URL=https://your-frontend-domain.com,https://www.your-frontend-domain.com
